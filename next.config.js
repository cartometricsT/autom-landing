/** @type {import('next').NextConfig} */
const nextConfig = {
    target: 'serverless', // Aquí se define la opción target
    // Otras configuraciones pueden ir aquí
  };
  
  module.exports = nextConfig;